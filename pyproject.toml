[project]
name = "rage-cage-arm-hackstorm"
version = "0.1.0"
description = "Rage Cage robotic arm utilities"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "numpy>=1.24",
  "opencv-contrib-python>=4.8",
  "websocket-client>=1.7",
]

[tool.uv]
package = false
